import { redirect } from "next/navigation";

/**
 * `/payouts` is folded into the broader `/wallet` view. Keep this route as a
 * permanent redirect so old bookmarks, emails, and nav entries still work.
 */
export default function PayoutsRedirect() {
  redirect("/wallet");
}
